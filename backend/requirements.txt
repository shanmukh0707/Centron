# Sentinel backend. Everything here is pure Python or ships ARM wheels, so a
# Raspberry-Pi-class box installs it without a compiler.
pydantic>=2.6
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12
pyyaml>=6
anthropic>=1.0

# tests
pytest>=8
